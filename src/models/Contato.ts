class Contato {
  nome: string
  email: string
  phone: string
  id: number

  constructor(nome: string, email: string, phone: string, id: number) {
    this.nome = nome
    this.email = email
    this.phone = phone
    this.id = id
  }
}

export default Contato
